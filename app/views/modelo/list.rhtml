<div id="content">
	<div>
		<div class="content">
			<h1 class="title style1">Modelos de Documentos</h1>
			<br />
			<table border>
				<tr align="center">
					<th>Título</th>
					<% if session[:user].admin %>
					<th>Excluir</th>
					<% end %>
				</tr>
				<% for modelo in @modelos %>
				<tr align="center">
					<td><%= link_to modelo.titulo, { :controller => "modelo", :action => 'getModelo', :id => modelo }, :method => "post" %></td>
					<% if session[:user].admin %>
					<td><%= link_to image_tag("del.png", :border => 0, :alt => 'Apagar'), { :action => 'destroy', :id => modelo }, :confirm => 'Tem certeza que deseja excluir este modelo?', :method => "post" %></td>
					<% end %>
				</tr>
				<% end %>
			</table>
			<p>
				<%= link_to image_tag("PreviousPage.gif", :border => 0, :alt => 'Anterior'), { :page => @modelo_pages.current.previous } if @modelo_pages.current.previous %>
				<% for page in @modelo_pages %>
				    &nbsp;<%= link_to_unless(params[:page].to_i == page.number, page.number, {:params => params.merge('page' => page)}) %>&nbsp;
				<% end -%>
				<%= link_to image_tag("NextPage.gif", :border => 0, :alt => 'Próxima'), { :page => @modelo_pages.current.next } if @modelo_pages.current.next %>
			</p>
			<p>
			<% if session[:user].admin %>
				<%= link_to image_tag("add.png", :border => 0, :alt => 'Adicionar modelo'), {:action => 'new'} %> | 
			<% end %>
				<%= link_to image_tag("goBack.gif", :border => 0, :alt => 'Voltar'), {:controller => "user", :action => 'welcome'} %>
			</p>
	  	</div>
	</div>
</div>
